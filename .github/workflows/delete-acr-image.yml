name: Delete - Docker image from ACR

on:
  workflow_call:
    inputs:
      imageTags:
        type: string
        description: "Image Tags or PR numbers"
        required: true
  workflow_dispatch:
    inputs:
      imageTag:
        description: "Images Tag or PR number"
        required: true

defaults:
  run:
    shell: pwsh

env:
  IMAGE_TAG: ${{ inputs.imageTags || inputs.imageTag }}
  PREFIX: pr-
permissions:
  id-token: write
  contents: read

jobs:
  delete-acr-image:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        IMAGE_TAG: ${{ fromJson(inputs.imageTags) }}

    steps:
      - uses: actions/checkout@v4

      - name: Load .env file
        uses: xom9ikk/dotenv@v2
        with:
          path: ./.github

      - name: Azure CLI - Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: ACR - Login
        run: |
          az acr login --name ${{ env.ACR_LOGIN_SERVER }}

      - name: ACR - Delete image
        run: |
          if('${{ matrix.IMAGE_TAG }}'.Length -le 4) # PR tags contain 4 digits
          {
            $imageTagWithPrefix = '${{ env.PREFIX }}${{ matrix.IMAGE_TAG}}'
          }else{
            $imageTagWithPrefix = '${{ matrix.IMAGE_TAG }}'
          }
          az acr repository delete --name ${{ env.ACR_NAME }} `
          --image ${{ env.IMAGE_NAME }}:$imageTagWithPrefix --yes
          Write-Output "✅ ACR - ${{ env.IMAGE_NAME }}:$imageTagWithPrefix image deleted successfully."
